#include <stdio.h>
#include <stdlib.h>

int main() {
    char *token = "a";
    printf("%d",atoi("-") );
    switch (token[0]) {
        case 'a':
            printf("Token is 'a'\n");
            break;
    }
    return 0;
}